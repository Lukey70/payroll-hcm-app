v1.1.25

Reviewed changes:
- Blank End Date support for continuous Union Fees deductions.
- Multiple concurrent deductions, including overlapping records of the same deduction type.
- Payslip Annual/Personal Leave closing balances include current-pay leave usage, remain stable on recalculation and are preserved on finalisation.

Validation:
- Full Node regression suite passed (96 self-invoking test blocks; 410 assertion invocations; 46 PASS summary lines).
- JavaScript syntax checks passed for all four JS files.
- Simulated/non-browser interface coverage passed where included in the suite.
- Chromium 144 real-browser smoke attempt reached the local app with HTTP 200 but did not return a rendered DOM before timeout; no real-browser pass is claimed.

Known limitations are recorded in RELEASE-NOTES-v1.1.25.txt.
